import {task, TaskSet, TaskExecutionContext} from "../../../../../core/tasks"

export class MyBuildTasks extends TaskSet {
  // another way of expressing selectors
  // app.entities[id:"myEntity"].fields[id:"myField"]
  @task("app")
  MyAppTask(context:TaskExecutionContext){
    return "test1";
  }
}
